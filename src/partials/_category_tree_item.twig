{% if category is defined %}
  {% if category.children | length > 0 %}
    <li class="sidebar__sidebar-item">
      <a class="sidebar__sidebar-list-title" href="/{{ parent }}/{{ category.permalink }}">{{ category.title }}</a>
      <a class="sidebar__sidebar-list-toggle" href="#"><i class="mdi mdi-chevron-up" aria-hidden="true"></i></a>
    <ul class="sidebar__sidebar-submenu {% if category.permalink == current.permalink %}active{% endif %}">
    {% for sub_category in category.children %}
      {% include '_category_tree_item.twig' with {
        'category' : sub_category,
        'current'  : current,
        'parent'  : 'categorias'
      } only %}
    {% endfor %}
    </ul>
    </li>
  {% else %}
    <li class="sidebar__sidebar-item">
      <a class="sidebar__sidebar-list-title" href="/{{ parent }}/{{ category.permalink }}">{{ category.title }}</a>
    </li>
  {% endif %}
{% endif %}
